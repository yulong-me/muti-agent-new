# Local dev: frontend connects to backend at this URL.
# For same-origin proxy (next.config.js rewrites /api → backend): leave empty.
# For separate processes on different hosts: set to http://<backend-host>:7001
NEXT_PUBLIC_API_URL=
